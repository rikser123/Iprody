version: "3"

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: iprody
    ports:
      - '5432:5432'
    volumes:
       - database:/var/lib/postgresql/data

volumes: 
  database: